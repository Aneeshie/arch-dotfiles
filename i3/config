# ================================
# PRODUCTIVITY-FOCUSED i3 CONFIG
# Catppuccin Theme | 2560x1440@180hz Optimized
# ================================

# Window appearance
for_window [class="^.*"] border pixel 2
gaps inner 6
gaps outer 4
smart_gaps on
smart_borders on

set $mod Mod4

# ================================
# EXTREME PERFORMANCE AUTOSTART
# ================================

# RTX 4060 + NO IGPU = HYPRLAND DESTROYER MODE!
exec_always --no-startup-id picom --config ~/.config/picom/picom.conf --no-fading-openclose --experimental-backends
exec --no-startup-id feh --bg-scale ~/.config/catppuccin-wallpaper.png
exec --no-startup-id xrdb -merge ~/.Xresources
exec_always --no-startup-id setxkbmap -option caps:escape
# FIXED: Previous setting was too aggressive and caused double typing
# exec --no-startup-id xset r rate 100 25  # EXTREME key repeat for speed demons - CAUSED DOUBLE TYPING
exec --no-startup-id xset r rate 300 35  # Fast but reasonable key repeat (300ms delay, ~28 repeats/sec)
exec --no-startup-id xrandr --output DP-1 --mode 2560x1440 --rate 180 --primary

# RTX 4060 NUCLEAR PERFORMANCE MODE
exec --no-startup-id echo performance | sudo tee /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor
exec --no-startup-id nvidia-settings -a "[gpu:0]/GPUPowerMizerMode=1"  # RTX max performance
exec --no-startup-id nvidia-settings -a "[gpu:0]/GPUMemoryTransferRateOffset[3]=1000"  # Memory OC
exec --no-startup-id nvidia-settings -a "[gpu:0]/GPUGraphicsClockOffset[3]=100"   # Core OC

# Preload common applications for instant launches
exec --no-startup-id zen-browser --no-startup-window --disable-extensions
exec --no-startup-id ghostty --daemon
exec --no-startup-id code --disable-extensions --no-sandbox

# Productivity tools autostart
exec --no-startup-id dunst  # Notifications
exec --no-startup-id redshift -l 40.7128:-74.0060  # Blue light filter
exec --no-startup-id copyq  # Advanced clipboard manager
exec --no-startup-id flameshot  # Screenshot tool
exec --no-startup-id solaar -w hide  # Logitech device manager
exec --no-startup-id blueman-applet  # Bluetooth manager

# System monitoring in background
exec --no-startup-id htop
exec --no-startup-id iotop

# Polybar
exec_always killall polybar
exec_always --no-startup-id ~/.config/polybar/launch_polybar.sh

# ================================
# SCREENSHOT BINDINGS
# ================================
bindsym Print exec maim "/home/$USER/Pictures/$(date).jpg"
bindsym $mod+Print exec maim --window $(xdotool getactivewindow) "/home/$USER/Pictures/$(date).jpg"
bindsym Shift+Print exec maim --select "/home/$USER/Pictures/$(date).jpg"
bindsym $mod+p exec flameshot gui
bindsym $mod+Shift+p exec flameshot full -p ~/Pictures
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:MesloLGS Nerd Font Mono Bold 14

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl set +5%
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl set 5%-

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# ================================
# APPLICATION BINDINGS
# ================================

# Core applications
bindsym $mod+Return exec ghostty
bindsym $mod+Shift+Return exec ghostty --class="floating_terminal"
bindsym $mod+b exec zen-browser  # Zen browser as default
bindsym $mod+Shift+b exec firefox  # Firefox as backup

# Kill focused window
bindsym $mod+q kill
bindsym $mod+Shift+q exec xkill  # Force kill with mouse

# File managers (multiple options for productivity)
bindsym $mod+n exec ghostty -e ranger  # Ranger file manager
bindsym $mod+Shift+n exec nautilus     # GUI file manager
bindsym $mod+Control+n exec ghostty -e lf  # LF file manager (lightweight)

# Development tools
bindsym $mod+Control+d exec ghostty --class="lazydocker" -e lazydocker
bindsym $mod+Control+g exec ghostty --class="lazygit" -e lazygit
bindsym $mod+Control+h exec ghostty --class="htop" -e htop
bindsym $mod+Control+i exec ghostty --class="iotop" -e sudo iotop

# System monitors
bindsym $mod+Control+s exec ghostty -e systemctl status
bindsym $mod+Control+j exec ghostty -e journalctl -f

# Rofi menus (customized for productivity)
bindsym $mod+d exec --no-startup-id rofi -show drun -theme ~/.config/rofi/catppuccin-mocha.rasi
bindsym $mod+space exec --no-startup-id rofi -show combi -theme ~/.config/rofi/catppuccin-mocha.rasi
bindsym $mod+Tab exec --no-startup-id rofi -show window -theme ~/.config/rofi/catppuccin-mocha.rasi
bindsym $mod+c exec --no-startup-id rofi -show calc -theme ~/.config/rofi/catppuccin-mocha.rasi
bindsym $mod+v exec --no-startup-id copyq toggle  # Clipboard manager
bindsym $mod+Control+p exec --no-startup-id ~/.config/rofi/powermenu.sh
bindsym $mod+Control+o exec --no-startup-id ~/.config/rofi/productivity.sh  # Productivity menu

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# ================================
# WORKSPACE CONFIGURATION
# ================================

# Define workspace names with icons for productivity
set $ws1 "1:  Terminal"
set $ws2 "2:  Browser"
set $ws3 "3:  Code"
set $ws4 "4:  Files"
set $ws5 "5:  Docker"
set $ws6 "6:  Monitor"
set $ws7 "7:  Media"
set $ws8 "8:  Chat"
set $ws9 "9:  Notes"
set $ws10 "10:  System"

# Workspace assignments for automatic organization
assign [class="zen-browser"] $ws2
assign [class="firefox"] $ws2
assign [class="Code"] $ws3
assign [class="code-oss"] $ws3
assign [class="nvim"] $ws3
assign [class="Nautilus"] $ws4
assign [class="ranger"] $ws4
assign [class="lazydocker"] $ws5
assign [class="Docker Desktop"] $ws5
assign [class="htop"] $ws6
assign [class="iotop"] $ws6
assign [class="vlc"] $ws7
assign [class="spotify"] $ws7
assign [class="discord"] $ws8
assign [class="slack"] $ws8
assign [class="obsidian"] $ws9
assign [class="notion"] $ws9

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# ================================
# ADVANCED PRODUCTIVITY FEATURES
# ================================

# Window rules for floating windows
for_window [class="floating_terminal"] floating enable
for_window [class="lazydocker"] floating enable, resize set 1600 1000, move position center
for_window [class="htop"] floating enable, resize set 1200 800, move position center
for_window [class="copyq"] floating enable, resize set 800 600, move position center
for_window [class="Rofi"] floating enable
for_window [class="Calculator"] floating enable
for_window [title="Picture-in-Picture"] floating enable, sticky enable

# ================================
# LIGHTNING-FAST WINDOW MANAGEMENT
# FASTER THAN HYPRLAND!
# ================================

# INSTANT workspace switching (no animations, pure speed)
bindsym $mod+bracketleft workspace prev_on_output
bindsym $mod+bracketright workspace next_on_output
bindsym $mod+Control+Left workspace prev_on_output
bindsym $mod+Control+Right workspace next_on_output

# SPEED DEMON workspace jumping
bindsym $mod+grave workspace back_and_forth
bindsym $mod+Shift+grave move container to workspace back_and_forth

# ULTRA-FAST direct workspace access (bypassing animations)
bindsym $mod+F1 workspace number $ws1; focus
bindsym $mod+F2 workspace number $ws2; focus  
bindsym $mod+F3 workspace number $ws3; focus
bindsym $mod+F4 workspace number $ws4; focus
bindsym $mod+F5 workspace number $ws5; focus
bindsym $mod+F6 workspace number $ws6; focus
bindsym $mod+F7 workspace number $ws7; focus
bindsym $mod+F8 workspace number $ws8; focus
bindsym $mod+F9 workspace number $ws9; focus
bindsym $mod+F10 workspace number $ws10; focus

# INSTANT window focus switching (Hyprland killer)
bindsym $mod+Alt+h focus left; focus
bindsym $mod+Alt+j focus down; focus
bindsym $mod+Alt+k focus up; focus
bindsym $mod+Alt+l focus right; focus

# BLAZING window movement
bindsym $mod+Alt+Shift+h move left 50px
bindsym $mod+Alt+Shift+j move down 50px
bindsym $mod+Alt+Shift+k move up 50px
bindsym $mod+Alt+Shift+l move right 50px

# Scratchpad management (INSTANT access)
bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show

# SPEED-OPTIMIZED window cycling
bindsym $mod+comma focus prev; focus
bindsym $mod+period focus next; focus

# INSTANT container operations
bindsym $mod+semicolon focus parent; focus
bindsym $mod+apostrophe focus child; focus

# ================================
# HYPRLAND-DESTROYING SPEED BINDS
# ================================

# ULTRA-FAST application launches (faster than omarchy's Hyprland!)
bindsym $mod+Control+c exec ~/.config/scripts/ultra-fast-launcher.sh code
bindsym $mod+Control+f exec ~/.config/scripts/ultra-fast-launcher.sh files
bindsym $mod+Control+t exec ~/.config/scripts/ultra-fast-launcher.sh terminal
bindsym $mod+Control+r exec ~/.config/scripts/ultra-fast-launcher.sh files
bindsym $mod+Control+v exec ~/.config/scripts/ultra-fast-launcher.sh clipboard
bindsym $mod+Control+m exec ~/.config/scripts/ultra-fast-launcher.sh monitor

# INSTANT browser and development tools
bindsym $mod+Control+b exec ~/.config/scripts/ultra-fast-launcher.sh browser
bindsym $mod+Control+docker exec ~/.config/scripts/ultra-fast-launcher.sh docker
bindsym $mod+Control+git exec ~/.config/scripts/ultra-fast-launcher.sh git

# NUCLEAR-SPEED workspace + app combos (beat Hyprland!)
bindsym $mod+Alt+1 exec "i3-msg 'workspace 1; exec ~/.config/scripts/ultra-fast-launcher.sh terminal'"
bindsym $mod+Alt+2 exec "i3-msg 'workspace 2; exec ~/.config/scripts/ultra-fast-launcher.sh browser'"
bindsym $mod+Alt+3 exec "i3-msg 'workspace 3; exec ~/.config/scripts/ultra-fast-launcher.sh code'"
bindsym $mod+Alt+4 exec "i3-msg 'workspace 4; exec ~/.config/scripts/ultra-fast-launcher.sh files'"
bindsym $mod+Alt+5 exec "i3-msg 'workspace 5; exec ~/.config/scripts/ultra-fast-launcher.sh docker'"

# SPEED DEMON performance testing
bindsym $mod+Control+Shift+b exec ~/.config/scripts/hyprland-killer-benchmark.sh

# RTX 4060 BEAST MODE ACTIVATION
bindsym $mod+Control+Shift+r exec ghostty --class="rtx_beast_mode" -e ~/.config/scripts/rtx4060-beast-mode.sh

# System control shortcuts (INSTANT response)
bindsym $mod+Control+l exec i3lock -c 1e1e2e --no-unlock-indicator --ignore-empty-password
bindsym $mod+Control+k exec systemctl suspend

# ================================
# CATPPUCCIN MOCHA COLOR SCHEME
# ================================

# Catppuccin Mocha Colors
set $rosewater #f5e0dc
set $flamingo  #f2cdcd
set $pink      #f5c2e7
set $mauve     #cba6f7
set $red       #f38ba8
set $maroon    #eba0ac
set $peach     #fab387
set $yellow    #f9e2af
set $green     #a6e3a1
set $teal      #94e2d5
set $sky       #89dceb
set $sapphire  #74c7ec
set $blue      #89b4fa
set $lavender  #b4befe
set $text      #cdd6f4
set $subtext1  #bac2de
set $subtext0  #a6adc8
set $overlay2  #9399b2
set $overlay1  #7f849c
set $overlay0  #6c7086
set $surface2  #585b70
set $surface1  #45475a
set $surface0  #313244
set $base      #1e1e2e
set $mantle    #181825
set $crust     #11111b

# Window colors
# target                 title     bg        text        indicator   border
client.focused           $lavender $base     $text       $rosewater  $lavender
client.focused_inactive  $overlay0 $base     $text       $rosewater  $overlay0
client.unfocused         $overlay0 $base     $text       $rosewater  $overlay0
client.urgent            $peach    $base     $peach      $overlay0   $peach
client.placeholder       $overlay0 $base     $text       $overlay0   $overlay0
client.background        $base
